services:
  - type: web
    name: zamowienia-magazyn
    runtime: python
    pythonVersion: "3.9"  # Wymuś starszą wersję Pythona
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:app